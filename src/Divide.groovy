class Adder {
  try { def Divide(int x, int y) {
        return x/y
    }
      }
  catch (ArithmeticException e) {
            // Exception handler
            SSystem.out.println(e.getMessage());
        }
    }
}
