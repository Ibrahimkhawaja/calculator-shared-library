class Divide {
  try { def Div(int x, int y) 
        return x/y
    }
      
  catch (ArithmeticException e) {
            // Exception handler
            SSystem.out.println(e.getMessage());
        }
    
}
